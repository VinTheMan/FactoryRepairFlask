# 主機IP
修改檔案 : nginx/nginx.conf

# 製作image
# sudo docker-compose --env-file docker.env build

# 執行/取消 container
# sudo docker-compose --env-file docker.env up
# sudo docker-compose --env-file docker.env down